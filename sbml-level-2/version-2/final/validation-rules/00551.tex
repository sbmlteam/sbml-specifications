When a value for \token{delay} is given in a \Event definition, the units
of the delay formula must correspond to either the value of
\token{timeUnits} in the \Event or (if no \token{timeUnits} are given), the
model's default units of time.  (References: L2V2
Section~\ref{sec:event-delay}.)
