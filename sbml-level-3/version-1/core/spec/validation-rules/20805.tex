An \InitialAssignment object must have the required attribute 
\token{symbol} and may have the following optional attributes:
\token{metaid} and \token{sboTerm}.  No other attributes are permitted.  
(References: SBML L3V1 Section~\ref{sec:initialAssignment}.)
