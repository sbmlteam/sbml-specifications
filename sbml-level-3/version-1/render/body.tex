% -*- TeX-master: "main"; fill-column: 72 -*-
\section{Package syntax and semantics}

In this section, we define the syntax and semantics of the
\RenderPackage for \sbmlthreecore. We expound on the various data types
and constructs defined in this package, then in \sec{examples}, we
provide complete examples of using the constructs in example SBML
models.


% ------------------------------------------------------------------------------------------
\subsection{The extended \class{GraphicalObject} class}

% TO DO: finish code for extended GraphicalObject

% ------------------------------------------------------------------------------------------
\subsection{The extended \class{Layout} class}

% TO DO: finish code for extended Layout

% ------------------------------------------------------------------------------------------
\subsection{The extended \class{ListOfLayouts} class}

% TO DO: finish code for extended ListOfLayouts

% ------------------------------------------------------------------------------------------
\subsection{The \class{ColorDefinition} class}
\label{colordefinition-class}

% TO DO: explain ColorDefinition

The \ColorDefinition object derives from the \class{SBase} class and
thus inherits any attributes and elements that are present on this
class.
In addition the \ColorDefinition object has the following attributes.

\paragraph{The \fixttspace\token{id} attribute}

A \ColorDefinition has an optional attribute \token{id} of type
\primtype{SId}.

\paragraph{The \fixttspace\token{value} attribute}

A \ColorDefinition has an optional attribute \token{value} of type
\primtype{string}.

% ------------------------------------------------------------------------------------------
\subsection{The \class{Ellipse} class}
\label{renderellipse-class}

% TO DO: explain Ellipse

The \RenderEllipse object derives from the \class{GraphicalPrimitive2D}
class and thus inherits any attributes and elements that are present on
this class.
In addition the \RenderEllipse object has the following attributes.

\paragraph{The \fixttspace\token{cx} attribute}

A \RenderEllipse has a required attribute \token{cx} of type
\RelAbsVector.

\paragraph{The \fixttspace\token{cy} attribute}

A \RenderEllipse has a required attribute \token{cy} of type
\RelAbsVector.

\paragraph{The \fixttspace\token{cz} attribute}

A \RenderEllipse has an optional attribute \token{cz} of type
\RelAbsVector.

\paragraph{The \fixttspace\token{rx} attribute}

A \RenderEllipse has a required attribute \token{rx} of type
\RelAbsVector.

\paragraph{The \fixttspace\token{ry} attribute}

A \RenderEllipse has an optional attribute \token{ry} of type
\RelAbsVector.

% ------------------------------------------------------------------------------------------
\subsection{The \class{GlobalRenderInformation} class}
\label{globalrenderinformation-class}

% TO DO: explain GlobalRenderInformation

The \GlobalRenderInformation object derives from the
\class{RenderInformationBase} class and thus inherits any attributes and
elements that are present on this class.
A \GlobalRenderInformation contains exactly one \token{ListOfStyles}
element.
In addition the \GlobalRenderInformation object has the following
attributes.

\TODO{deal with attribute versionMajor on a
listOfGlobalRenderInformation element}

\TODO{deal with attribute versionMinor on a
listOfGlobalRenderInformation element}

% ------------------------------------------------------------------------------------------
\subsection{The \class{GlobalStyle} class}
\label{globalstyle-class}

% TO DO: explain GlobalStyle

The \GlobalStyle object derives from the \class{Style} class and thus
inherits any attributes and elements that are present on this class.
% ------------------------------------------------------------------------------------------
\subsection{The \class{GradientBase} class}
\label{gradientbase-class}

% TO DO: explain GradientBase

The \GradientBase object derives from the \class{SBase} class and thus
inherits any attributes and elements that are present on this class.
A \GradientBase contains exactly one \token{ListOfStops} element.
In addition the \GradientBase object has the following attributes.

\paragraph{The \fixttspace\token{id} attribute}

A \GradientBase has a required attribute \token{id} of type
\primtype{SId}.

\paragraph{The \fixttspace\token{spreadMethod} attribute}

A \GradientBase has an optional attribute \token{spreadMethod} of type
\GradientSpreadMethod.

% ------------------------------------------------------------------------------------------
\subsection{The \class{GradientStop} class}
\label{gradientstop-class}

% TO DO: explain GradientStop

The \GradientStop object derives from the \class{SBase} class and thus
inherits any attributes and elements that are present on this class.
In addition the \GradientStop object has the following attributes.

\paragraph{The \fixttspace\token{offset} attribute}

A \GradientStop has a required attribute \token{offset} of type
\RelAbsVector.

\paragraph{The \fixttspace\token{stop-color} attribute}

A \GradientStop has a required attribute \token{stop-color} of type
\primtype{string}.

% ------------------------------------------------------------------------------------------
\subsection{The \class{RenderGroup} class}
\label{rendergroup-class}

% TO DO: explain RenderGroup

The \RenderGroup object derives from the \class{GraphicalPrimitive1D}
class and thus inherits any attributes and elements that are present on
this class.
A \RenderGroup contains exactly one TO DO: add type element.
In addition the \RenderGroup object has the following attributes.

\paragraph{The \fixttspace\token{startHead} attribute}

A \RenderGroup has an optional attribute \token{startHead} of type
\primtype{SIdRef}.

\paragraph{The \fixttspace\token{endHead} attribute}

A \RenderGroup has an optional attribute \token{endHead} of type
\primtype{SIdRef}.

\paragraph{The \fixttspace\token{font-family} attribute}

A \RenderGroup has an optional attribute \token{font-family} of type
\primtype{string}.

\paragraph{The \fixttspace\token{font-weight} attribute}

A \RenderGroup has an optional attribute \token{font-weight} of type
\FontWeight.

\paragraph{The \fixttspace\token{font-style} attribute}

A \RenderGroup has an optional attribute \token{font-style} of type
\FontStyle.

\paragraph{The \fixttspace\token{text-anchor} attribute}

A \RenderGroup has an optional attribute \token{text-anchor} of type
\HTextAnchor.

\paragraph{The \fixttspace\token{vtext-anchor} attribute}

A \RenderGroup has an optional attribute \token{vtext-anchor} of type
\VTextAnchor.

\paragraph{The \fixttspace\token{font-size} attribute}

A \RenderGroup has an optional attribute \token{font-size} of type
\RelAbsVector.

% ------------------------------------------------------------------------------------------
\subsection{The \class{Image} class}
\label{image-class}

% TO DO: explain Image

The \Image object derives from the \class{Transformation2D} class and
thus inherits any attributes and elements that are present on this
class.
In addition the \Image object has the following attributes.

\paragraph{The \fixttspace\token{id} attribute}

An \Image has an optional attribute \token{id} of type \primtype{SId}.

\paragraph{The \fixttspace\token{x} attribute}

An \Image has a required attribute \token{x} of type \RelAbsVector.

\paragraph{The \fixttspace\token{y} attribute}

An \Image has a required attribute \token{y} of type \RelAbsVector.

\paragraph{The \fixttspace\token{z} attribute}

An \Image has an optional attribute \token{z} of type \RelAbsVector.

\paragraph{The \fixttspace\token{width} attribute}

An \Image has a required attribute \token{width} of type \RelAbsVector.

\paragraph{The \fixttspace\token{height} attribute}

An \Image has a required attribute \token{height} of type \RelAbsVector.

\paragraph{The \fixttspace\token{href} attribute}

An \Image has a required attribute \token{href} of type
\primtype{string}.

% ------------------------------------------------------------------------------------------
\subsection{The \class{LineEnding} class}
\label{lineending-class}

% TO DO: explain LineEnding

The \LineEnding object derives from the \class{GraphicalPrimitive2D}
class and thus inherits any attributes and elements that are present on
this class.
A \LineEnding contains exactly one \token{RenderGroup} element.
A \LineEnding contains exactly one \token{BoundingBox} element.
In addition the \LineEnding object has the following attributes.

\paragraph{The \fixttspace\token{id} attribute}

A \LineEnding has a required attribute \token{id} of type
\primtype{SId}.

\paragraph{The \fixttspace\token{enableRotationalMapping} attribute}

A \LineEnding has an optional attribute \token{enableRotationalMapping}
of type \primtype{boolean}.

% ------------------------------------------------------------------------------------------
\subsection{The \class{LinearGradient} class}
\label{lineargradient-class}

% TO DO: explain LinearGradient

The \LinearGradient object derives from the \class{GradientBase} class
and thus inherits any attributes and elements that are present on this
class.
In addition the \LinearGradient object has the following attributes.

\paragraph{The \fixttspace\token{x1} attribute}

A \LinearGradient has an optional attribute \token{x1} of type
\RelAbsVector.

\paragraph{The \fixttspace\token{y1} attribute}

A \LinearGradient has an optional attribute \token{y1} of type
\RelAbsVector.

\paragraph{The \fixttspace\token{z1} attribute}

A \LinearGradient has an optional attribute \token{z1} of type
\RelAbsVector.

\paragraph{The \fixttspace\token{x2} attribute}

A \LinearGradient has an optional attribute \token{x2} of type
\RelAbsVector.

\paragraph{The \fixttspace\token{y2} attribute}

A \LinearGradient has an optional attribute \token{y2} of type
\RelAbsVector.

\paragraph{The \fixttspace\token{z2} attribute}

A \LinearGradient has an optional attribute \token{z2} of type
\RelAbsVector.

% ------------------------------------------------------------------------------------------
\subsection{The \class{LocalRenderInformation} class}
\label{localrenderinformation-class}

% TO DO: explain LocalRenderInformation

The \LocalRenderInformation object derives from the
\class{RenderInformationBase} class and thus inherits any attributes and
elements that are present on this class.
A \LocalRenderInformation contains exactly one \token{ListOfStyles}
element.
In addition the \LocalRenderInformation object has the following
attributes.

\TODO{deal with attribute versionMajor on a listOfLocalRenderInformation
element}

\TODO{deal with attribute versionMinor on a listOfLocalRenderInformation
element}

% ------------------------------------------------------------------------------------------
\subsection{The \class{LocalStyle} class}
\label{localstyle-class}

% TO DO: explain LocalStyle

The \LocalStyle object derives from the \class{Style} class and thus
inherits any attributes and elements that are present on this class.
In addition the \LocalStyle object has the following attributes.

\paragraph{The \fixttspace\token{idList} attribute}

A \LocalStyle has an optional attribute \token{idList} of type
\primtype{string}.

% ------------------------------------------------------------------------------------------
\subsection{The \class{Polygon} class}
\label{polygon-class}

% TO DO: explain Polygon

The \Polygon object derives from the \class{GraphicalPrimitive2D} class
and thus inherits any attributes and elements that are present on this
class.
A \Polygon contains at most one \token{ListOfElements} element.
In addition the \Polygon object has the following attributes.

% ------------------------------------------------------------------------------------------
\subsection{The \class{RadialGradient} class}
\label{radialgradient-class}

% TO DO: explain RadialGradient

The \RadialGradient object derives from the \class{GradientBase} class
and thus inherits any attributes and elements that are present on this
class.
In addition the \RadialGradient object has the following attributes.

\paragraph{The \fixttspace\token{cx} attribute}

A \RadialGradient has an optional attribute \token{cx} of type
\RelAbsVector.

\paragraph{The \fixttspace\token{cy} attribute}

A \RadialGradient has an optional attribute \token{cy} of type
\RelAbsVector.

\paragraph{The \fixttspace\token{cz} attribute}

A \RadialGradient has an optional attribute \token{cz} of type
\RelAbsVector.

\paragraph{The \fixttspace\token{r} attribute}

A \RadialGradient has an optional attribute \token{r} of type
\RelAbsVector.

\paragraph{The \fixttspace\token{fx} attribute}

A \RadialGradient has an optional attribute \token{fx} of type
\RelAbsVector.

\paragraph{The \fixttspace\token{fy} attribute}

A \RadialGradient has an optional attribute \token{fy} of type
\RelAbsVector.

\paragraph{The \fixttspace\token{fz} attribute}

A \RadialGradient has an optional attribute \token{fz} of type
\RelAbsVector.

% ------------------------------------------------------------------------------------------
\subsection{The \class{Rectangle} class}
\label{renderrectangle-class}

% TO DO: explain Rectangle

The \RenderRectangle object derives from the
\class{GraphicalPrimitive2D} class and thus inherits any attributes and
elements that are present on this class.
In addition the \RenderRectangle object has the following attributes.

\paragraph{The \fixttspace\token{x} attribute}

A \RenderRectangle has a required attribute \token{x} of type
\RelAbsVector.

\paragraph{The \fixttspace\token{y} attribute}

A \RenderRectangle has an optional attribute \token{y} of type
\RelAbsVector.

\paragraph{The \fixttspace\token{z} attribute}

A \RenderRectangle has an optional attribute \token{z} of type
\RelAbsVector.

\paragraph{The \fixttspace\token{width} attribute}

A \RenderRectangle has a required attribute \token{width} of type
\RelAbsVector.

\paragraph{The \fixttspace\token{height} attribute}

A \RenderRectangle has a required attribute \token{height} of type
\RelAbsVector.

\paragraph{The \fixttspace\token{rX} attribute}

A \RenderRectangle has an optional attribute \token{rX} of type
\RelAbsVector.

\paragraph{The \fixttspace\token{rY} attribute}

A \RenderRectangle has an optional attribute \token{rY} of type
\RelAbsVector.

% ------------------------------------------------------------------------------------------
\subsection{The \class{RenderCubicBezier} class}
\label{rendercubicbezier-class}

% TO DO: explain RenderCubicBezier

The \RenderCubicBezier object derives from the \class{RenderPoint} class
and thus inherits any attributes and elements that are present on this
class.
In addition the \RenderCubicBezier object has the following attributes.

\paragraph{The \fixttspace\token{basepoint1\_x} attribute}

A \RenderCubicBezier has a required attribute \token{basepoint1\_x} of
type \RelAbsVector.

\paragraph{The \fixttspace\token{basepoint1\_y} attribute}

A \RenderCubicBezier has a required attribute \token{basepoint1\_y} of
type \RelAbsVector.

\paragraph{The \fixttspace\token{basepoint1\_z} attribute}

A \RenderCubicBezier has an optional attribute \token{basepoint1\_z} of
type \RelAbsVector.

\paragraph{The \fixttspace\token{basepoint2\_x} attribute}

A \RenderCubicBezier has a required attribute \token{basepoint2\_x} of
type \RelAbsVector.

\paragraph{The \fixttspace\token{basepoint2\_y} attribute}

A \RenderCubicBezier has a required attribute \token{basepoint2\_y} of
type \RelAbsVector.

\paragraph{The \fixttspace\token{basepoint2\_z} attribute}

A \RenderCubicBezier has an optional attribute \token{basepoint2\_z} of
type \RelAbsVector.

% ------------------------------------------------------------------------------------------
\subsection{The \class{RenderCurve} class}
\label{rendercurve-class}

% TO DO: explain RenderCurve

The \RenderCurve object derives from the \class{GraphicalPrimitive1D}
class and thus inherits any attributes and elements that are present on
this class.
A \RenderCurve contains at most one \token{ListOfElements} element.
In addition the \RenderCurve object has the following attributes.

\paragraph{The \fixttspace\token{startHead} attribute}

A \RenderCurve has an optional attribute \token{startHead} of type
\primtype{SIdRef}.

\paragraph{The \fixttspace\token{endHead} attribute}

A \RenderCurve has an optional attribute \token{endHead} of type
\primtype{SIdRef}.

% ------------------------------------------------------------------------------------------
\subsection{The \class{RenderPoint} class}
\label{renderpoint-class}

% TO DO: explain RenderPoint

The \RenderPoint object derives from the \class{SBase} class and thus
inherits any attributes and elements that are present on this class.
In addition the \RenderPoint object has the following attributes.

\paragraph{The \fixttspace\token{x} attribute}

A \RenderPoint has a required attribute \token{x} of type \RelAbsVector.

\paragraph{The \fixttspace\token{y} attribute}

A \RenderPoint has a required attribute \token{y} of type \RelAbsVector.

\paragraph{The \fixttspace\token{z} attribute}

A \RenderPoint has an optional attribute \token{z} of type
\RelAbsVector.

% ------------------------------------------------------------------------------------------
\subsection{The \class{Text} class}
\label{text-class}

% TO DO: explain Text

The \Text object derives from the \class{GraphicalPrimitive1D} class and
thus inherits any attributes and elements that are present on this
class.
In addition the \Text object has the following attributes.

\paragraph{The \fixttspace\token{x} attribute}

A \Text has a required attribute \token{x} of type \RelAbsVector.

\paragraph{The \fixttspace\token{y} attribute}

A \Text has a required attribute \token{y} of type \RelAbsVector.

\paragraph{The \fixttspace\token{z} attribute}

A \Text has an optional attribute \token{z} of type \RelAbsVector.

\paragraph{The \fixttspace\token{font-family} attribute}

A \Text has an optional attribute \token{font-family} of type
\FontFamily.

\paragraph{The \fixttspace\token{font-weight} attribute}

A \Text has an optional attribute \token{font-weight} of type
\FontWeight.

\paragraph{The \fixttspace\token{font-style} attribute}

A \Text has an optional attribute \token{font-style} of type \FontStyle.

\paragraph{The \fixttspace\token{text-anchor} attribute}

A \Text has an optional attribute \token{text-anchor} of type
\HTextAnchor.

\paragraph{The \fixttspace\token{vtext-anchor} attribute}

A \Text has an optional attribute \token{vtext-anchor} of type
\VTextAnchor.

\paragraph{The \fixttspace\token{font-size} attribute}

A \Text has an optional attribute \token{font-size} of type
\RelAbsVector.

% ------------------------------------------------------------------------------------------
\subsection{The \class{Transformation2D} class}
\label{transformationtwod-class}

% TO DO: explain Transformation2D

The \TransformationTwoD object derives from the \class{Transformation}
class and thus inherits any attributes and elements that are present on
this class.
In addition the \TransformationTwoD object has the following attributes.

\paragraph{The \fixttspace\token{transform} attribute}

A \TransformationTwoD has an optional attribute \token{transform}
consisting of an array of \primtype{double}.

% ------------------------------------------------------------------------------------------
\subsection{The \class{Transformation} class}
\label{transformation-class}

% TO DO: explain Transformation

The \Transformation object derives from the \class{SBase} class and thus
inherits any attributes and elements that are present on this class.
In addition the \Transformation object has the following attributes.

\paragraph{The \fixttspace\token{transform} attribute}

A \Transformation has a required attribute \token{transform} consisting
of an array of \primtype{double}.

% ------------------------------------------------------------------------------------------
\subsection{The \class{GraphicalPrimitive1D} class}
\label{graphicalprimitiveoned-class}

% TO DO: explain GraphicalPrimitive1D

The \GraphicalPrimitiveOneD object derives from the
\class{Transformation2D} class and thus inherits any attributes and
elements that are present on this class.
In addition the \GraphicalPrimitiveOneD object has the following
attributes.

\paragraph{The \fixttspace\token{id} attribute}

A \GraphicalPrimitiveOneD has an optional attribute \token{id} of type
\primtype{SId}.

\paragraph{The \fixttspace\token{stroke} attribute}

A \GraphicalPrimitiveOneD has an optional attribute \token{stroke} of
type \primtype{string}.

\paragraph{The \fixttspace\token{stroke-width} attribute}

A \GraphicalPrimitiveOneD has an optional attribute \token{stroke-width}
of type \primtype{string}.

\paragraph{The \fixttspace\token{stroke-dasharray} attribute}

A \GraphicalPrimitiveOneD has an optional attribute
\token{stroke-dasharray} consisting of an array of \primtype{unsigned
integer}.

% ------------------------------------------------------------------------------------------
\subsection{The \class{GraphicalPrimitive2D} class}
\label{graphicalprimitivetwod-class}

% TO DO: explain GraphicalPrimitive2D

The \GraphicalPrimitiveTwoD object derives from the
\class{GraphicalPrimitive1D} class and thus inherits any attributes and
elements that are present on this class.
In addition the \GraphicalPrimitiveTwoD object has the following
attributes.

\paragraph{The \fixttspace\token{fill} attribute}

A \GraphicalPrimitiveTwoD has an optional attribute \token{fill} of type
\primtype{string}.

\paragraph{The \fixttspace\token{fill-rule} attribute}

A \GraphicalPrimitiveTwoD has an optional attribute \token{fill-rule} of
type \FillRule.

% ------------------------------------------------------------------------------------------
\subsection{The \class{Style} class}
\label{style-class}

% TO DO: explain Style

The \Style object derives from the \class{SBase} class and thus inherits
any attributes and elements that are present on this class.
A \Style contains exactly one \token{RenderGroup} element.
In addition the \Style object has the following attributes.

\paragraph{The \fixttspace\token{id} attribute}

A \Style has an optional attribute \token{id} of type \primtype{SId}.

\paragraph{The \fixttspace\token{name} attribute}

A \Style has an optional attribute \token{name} of type
\primtype{string}.

\paragraph{The \fixttspace\token{roleList} attribute}

A \Style has an optional attribute \token{roleList} of type
\primtype{string}.

\paragraph{The \fixttspace\token{typeList} attribute}

A \Style has an optional attribute \token{typeList} of type
\primtype{string}.

% ------------------------------------------------------------------------------------------
\subsection{The \class{RenderInformationBase} class}
\label{renderinformationbase-class}

% TO DO: explain RenderInformationBase

The \RenderInformationBase object derives from the \class{SBase} class
and thus inherits any attributes and elements that are present on this
class.
A \RenderInformationBase contains exactly one
\token{ListOfColorDefinitions} element.
A \RenderInformationBase contains exactly one
\token{ListOfGradientDefinitions} element.
A \RenderInformationBase contains exactly one \token{ListOfLineEndings}
element.
In addition the \RenderInformationBase object has the following
attributes.

\paragraph{The \fixttspace\token{id} attribute}

A \RenderInformationBase has a required attribute \token{id} of type
\primtype{SId}.

\paragraph{The \fixttspace\token{name} attribute}

A \RenderInformationBase has an optional attribute \token{name} of type
\primtype{string}.

\paragraph{The \fixttspace\token{programName} attribute}

A \RenderInformationBase has an optional attribute \token{programName}
of type \primtype{string}.

\paragraph{The \fixttspace\token{programVersion} attribute}

A \RenderInformationBase has an optional attribute
\token{programVersion} of type \primtype{string}.

\paragraph{The \fixttspace\token{refenceRenderInformation} attribute}

A \RenderInformationBase has an optional attribute
\token{refenceRenderInformation} of type \primtype{string}.

\paragraph{The \fixttspace\token{backgroundColor} attribute}

A \RenderInformationBase has an optional attribute
\token{backgroundColor} of type \primtype{string}.

