(Rule does not apply in SBML \thisLV.)

% In SBML Level~2 prior to Version~3, if a \Species is located in a
% \Compartment whose \token{spatialDimensions} has value \val{1}, then that
% \Species definition can only set \token{spatialSizeUnits} to a value of
% \val{length}, \val{metre}, \val{dimensionless}, or the identifier of a
% \UnitDefinition derived from \token{metre} (with an \token{exponent} value
% of \val{1}) or \val{dimensionless}.  (\token{spatialSizeUnits} has been
% removed in SBML \thisLV.  References: L2V1 Section 4.6.4; L2V2
% Section~\ref{sec:species-units}.)
