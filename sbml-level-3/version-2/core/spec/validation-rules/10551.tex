In an \Event object, the unit of measurement associated with a \Delay
object's \token{math} expression object should be identical to the unit
indicated by the \Model object's \token{timeUnits} attribute.  (References:
\tworefs{4.12.4}{\ref{sec:event-delay}}.)
