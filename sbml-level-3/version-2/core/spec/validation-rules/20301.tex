The top-level element within the MathML \token{math} element in a
\FunctionDefinition object, \changed{if present,} must be either exactly one MathML \token{lambda}
element, or exactly one MathML \token{semantics} element containing exactly
one \token{lambda} element.  (References: SBML L3V2
\sec{sec:function-definition-math}.)
