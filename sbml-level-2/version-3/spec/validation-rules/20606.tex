\begin{blockChanged}
(Rule does not apply in SBML Level~2 Version~3.)
\end{blockChanged}

% In SBML Level~2 prior to Version~3, if a \Species is located in a
% \Compartment whose \token{spatialDimensions} has value \val{2}, then that
% \Species definition can only set \token{spatialSizeUnits} to a value of
% \val{area}, \val{dimensionless}, or the identifier of a \UnitDefinition
% derived from either \token{metre} (with an \token{exponent} value of
% \val{2}) or \val{dimensionless}.  (\token{spatialSizeUnits} has been
% removed in SBML Level~2 Version~3.  References: L2V1 Section 4.6.4; L2V2
% Section~\ref{sec:species-units}.)
