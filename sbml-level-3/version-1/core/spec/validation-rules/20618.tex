The \Parameter object referenced by a \Species instance's
\token{conversionFactor} must have a value of \val{true} for its
\token{constant} attribute.  (References: SBML L3V1
Section~\ref{sec:species-conversion}.)
