Apart from the general notes and annotation subobjects permitted on all
SBML components, the \token{sbml} container object can only contain one
\Model object.  (References: SBML L3V1 Section~\ref{sec:listof}.)
