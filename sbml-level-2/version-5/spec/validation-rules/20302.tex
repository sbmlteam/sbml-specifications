(Rule does not apply in SBML \thisLV.)

% Inside the \token{lambda} of a \FunctionDefinition, if a
% \token{ci} element is the first element within a MathML
% \token{apply}, then the \token{ci}'s value can only be chosen from
% the set of identifiers of other SBML \FunctionDefinition{}s
% defined prior to that point in the SBML model.  In other words,
% forward references to user-defined functions are not permitted.
% (References: L2V2 Section~3.5.3; L2V3
% Section~3.4.3; L2V5 Section~\ref{sec:ci-token}.)
