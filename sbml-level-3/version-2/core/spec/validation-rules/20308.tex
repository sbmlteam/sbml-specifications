\changed{Inside the \token{lambda} MathML element within a \FunctionDefinition, \token{bvar} elements must contain exactly one instance of a \token{ci} element. No other elements are permitted within a \token{bvar} element.  (References: \tworefs{4.3.2}{\ref{sec:function-definition-math}}.)}
