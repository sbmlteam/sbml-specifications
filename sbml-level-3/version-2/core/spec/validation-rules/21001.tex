\changed{(This validation rule does not apply in SBML \thisLV.)}\footnote{\changed{Original rule:  ``The MathML \token{math} element in a \Constraint object must evaluate to a
value of type \primtype{boolean}.''}}
