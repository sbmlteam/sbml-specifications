MathML \token{lambda} elements are only permitted as the first element
inside the \token{math} element of a \FunctionDefinition \changed{or as the
first element of a \token{semantics} element immediately inside the
\token{math} element of a \FunctionDefinition}; they may not be used
elsewhere in an SBML model.  (References: L2V2 Sections~3.5.1 and~4.3;
\changed{L2V3 Sections~\ref{sec:mathmlsubset}
and~\ref{sec:function-definition-math}.})

