A \Species object cannot have values for both its
\token{initialConcentration} and \token{initialAmount} attributes because
these attributes are mutually exclusive.  (References: \tworefs{4.6.4}{\ref{sec:initialAmount}}.)
