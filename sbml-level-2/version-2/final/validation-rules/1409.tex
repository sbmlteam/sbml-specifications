A \Species cannot set values for both \token{initialConcentration} and
\token{initialAmount} because they are mutually exclusive.  (References:
L2V1 Section 4.6.3; L2V2 Section~\ref{sec:initialAmount}.)
