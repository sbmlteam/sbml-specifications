The contents of the \token{message} element must not contain an XML XML
DOCTYPE declaration (i.e., a string beginning with the characters
\val{<!DOCTYPE}.  (References: L2V3 Section~\ref{sec:constraint-message}.)
