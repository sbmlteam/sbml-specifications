Where the \token{substanceUnits} attribute on a \Species has 
not been set the units
are the \token{substanceUnits} declared on the \Model.  If neither
value is set, unit consistency cannot be checked.  (References: 
L3V1 Section~\ref{sec:species-units}.)
