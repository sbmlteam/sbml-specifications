The following MathML elements must yield numeric expressions: \token{math}
in \KineticLaw, \token{stoichiometryMath} in \SpeciesReference,
\token{math} in \InitialAssignment, \token{math} in \AssignmentRule,
\token{math} in \RateRule, \token{math} in \AlgebraicRule,
and \token{delay} in \Event, and \token{math} in \EventAssignment.


