The units of the expressions used as arguments to a function call
\changed{should} match the units expected for the arguments of that function.
(References: L2V2 Section~3.5; L2V3 Section~\ref{sec:formulas}.)
