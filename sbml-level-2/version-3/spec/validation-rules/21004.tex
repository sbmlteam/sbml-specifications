The contents of the \token{message} element must not contain an XML
declaration (i.e., a string of the form \val{<?xml version="1.0"
encoding="UTF-8"?>}).  (References: \changed{L2V3
Section~\ref{sec:constraint-message}.})
