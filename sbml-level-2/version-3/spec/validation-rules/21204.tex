\begin{blockChanged}
(Rule does not apply in SBML Level~2 Version~3.)
\end{blockChanged}

% In SBML Level~2 prior to Version~3, the value of an \Event's
% \token{timeUnits} must be \val{time}, \val{second}, \val{dimensionless}, or
% the identifier of a \UnitDefinition derived from either \token{second}
% (with an \token{exponent} value of \val{1}) or \val{dimensionless}.
% (\token{timeUnits} has been removed in SBML Level~2 Version~3. References:
% L2V1 Section 4.10.4; L2V2 Section~\ref{sec:event-timeunits}.)
