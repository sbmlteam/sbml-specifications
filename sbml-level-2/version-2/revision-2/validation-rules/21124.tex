The \token{constant} field on a \Parameter local to a \KineticLaw cannot
have a value other than \val{true}.  The values of parameters local to
\KineticLaw definitions cannot be changed, and therefore they are always
constant.  (References: L2V2 Section~\ref{subsec:kinetic-law}.)
