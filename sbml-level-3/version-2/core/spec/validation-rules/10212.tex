The types of the values within MathML \token{piecewise} operators must all
be consistent; \ie the set of expressions that make up the first
arguments of the \token{piece} and \token{otherwise} operators within the
same \token{piecewise} operator must all return values of the same type.
(References: SBML L3V2 Section~\ref{sec:mathmltype}.)
