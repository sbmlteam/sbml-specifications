An \Event object must have the required attribute 
\token{useValuesFromTriggerTime} and may have the following optional attributes:
\token{metaid}, \token{sboTerm}, \token{id} and \token{name}.  
No other attributes are permitted.  
(References: SBML L3V1 Section~\ref{sec:events}.)
