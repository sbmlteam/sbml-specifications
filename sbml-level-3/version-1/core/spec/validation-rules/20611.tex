A \Species having \token{boundaryCondition}=\val{false} cannot
appear as a reactant or product in any reaction if that \Species
also has \token{constant}=\val{true}.  (References: SBML
L3V1 Section~\ref{sec:species-constant}.)
