(Rule does not apply in SBML Level~2 Version~3.)

% In SBML Level~2 prior to Version~3, if a \Species is located in a
% \Compartment whose \token{spatialDimensions} has value \val{3}, then that
% \Species definition can only set \token{spatialSizeUnits} to a value of
% \val{volume}, \val{litre}, \val{dimensionless}, or the identifier of a
% \UnitDefinition derived from either \token{litre}, \token{metre} (with an
% \token{exponent} value of \val{3}) or \token{dimensionless}.
% (\token{spatialSizeUnits} has been removed in SBML Level~2 Version~3.
% References: L2V1 Section 4.6.4; L2V2 Section~\ref{sec:species-units}.)
