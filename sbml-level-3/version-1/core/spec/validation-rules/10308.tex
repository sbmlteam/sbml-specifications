The value of a \token{sboTerm} attribute must have the data type
\token{SBOTerm}, which is a string consisting of the characters `S', `B',
`O', ':', followed by exactly seven digits.  (References: SBML
L3V1 Section~\ref{sec:sboterm-type}.)
