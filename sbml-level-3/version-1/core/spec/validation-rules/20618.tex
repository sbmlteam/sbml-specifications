The value of \token{conversionFactor} in a \Species definition must be the
identifier of a \Parameter whose \token{constant} attribute has been set to
\val{true}.  (References: L3V1 Section~\ref{sec:species-conversion}.)