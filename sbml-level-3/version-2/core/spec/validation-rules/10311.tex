Unit identifiers (that is, the values of the \token{id} attribute on
\UnitDefinition, the \token{units} attribute on \Compartment, the
\token{units} attribute on \Parameter, \relchange{the \token{units} attribute on \LocalParameter,} the \token{substanceUnits} attribute
on \Species, the SBML \token{units} attribute on MathML \changed{\token{cn}}
elements, and the \token{substanceUnits}, \token{volumeUnits},
\token{areaUnits}, \token{lengthUnits}, \token{timeUnits} and
\token{extentUnits} on \Model) must always conform to the syntax of the
SBML data type \primtype{UnitSId}.  (References: \tworefs{3.1.9}{\ref{sec:unitsid}}.)
