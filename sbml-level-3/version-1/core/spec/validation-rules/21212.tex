Any \Compartment, \Species, \Parameter or \SpeciesReference object
definition whose identifier is used as the value of \token{variable} in an
\EventAssignment object must have a value of \val{false} for its
\token{constant} attribute.  (References: SBML L3V1
Section~\ref{sec:eventassignment}.)
