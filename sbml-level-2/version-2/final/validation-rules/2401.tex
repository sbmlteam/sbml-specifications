The \token{sbml} container element must declare the SBML Level and
Version using the attributes \token{level} and \token{version},
respectively, and this declaration must be consistent with the
XML Namespace declared for the \token{sbml} element.  (References: 
L2V2 Section~\ref{sec:sbml}.)

