The value of the \token{id} \changed{attribute} on every instance of the
following \changed{classes of objects in a model must} \changed{be unique
across the set of all \token{id} values in a model}: \changed{\Model,
\FunctionDefinition, \CompartmentType}, \changed{\SpeciesType,
\Compartment, \Species, \Reaction, \SpeciesReference,
\ModifierSpeciesReference, \Event,} \changed{and model-wide \Parameter{}s.
Exception: instances of \UnitDefinition, and parameters defined}
\changed{inside a reaction, are treated separately.}  (References: L2V1
Section~3.5; L2V2 Section~3.4.1; \changed{L2V3
Section~\ref{sec:idnameattribs}})
