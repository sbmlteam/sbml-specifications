For each \Species participating within a \Reaction the units of
the product of the effective \token{substanceUnits} and the effective 
\token{conversionFactor} for that \Species should be the 
equivalent of \token{extent}.
(References: L3V1 Section~\ref{sec:model-extentUnits}.)
