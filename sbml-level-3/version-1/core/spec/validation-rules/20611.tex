A \Species object having a \token{boundaryCondition} attribute value of
\val{false} cannot appear as a reactant or product in any reaction if that
\Species also has a \token{constant} attribute value of \val{true}.
(References: SBML L3V1 Section~\ref{sec:species-constant}.)
