The \Parameter object referenced by a \Species object's
\token{conversionFactor} attribute must have a value of \val{true} for its
attribute \token{constant}.  (References: SBML L3V1
Section~\ref{sec:species-conversion}.)
