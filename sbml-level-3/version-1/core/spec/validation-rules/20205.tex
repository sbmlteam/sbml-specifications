There can be at most one of each of the following container elements
in a \Model instance: \token{listOfFunctionDefinitions},
\token{listOfUnitDefinitions}, \token{listOfCompartments},
\token{listOfSpecies}, \token{listOfParameters},
\token{listOfInitialAssignments}, \token{listOfRules},
\token{listOfConstraints}, \\ \token{listOfReactions} and
\token{listOfEvents}.  (References: L3V1 Section~\ref{sec:model}.)
