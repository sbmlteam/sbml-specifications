Any \Compartment, \Species, \Parameter or \SpeciesReference object whose
identifier is the value of the attribute \token{variable} in a \RateRule
object, must have a value of \val{false} for its \token{constant}
attribute.  (References: SBML L3V1 Section~\ref{sec:raterule}.)
