The units of the mathematical formula in the \token{math} element of every
\KineticLaw object in a model should be identical to all \KineticLaw
objects in the model.  (References: L3V1 Section~\ref{sec:formulas}.)
