(Rule does not apply in SBML Level~2 Version~3.)

% In SBML Level~2 prior to Version~3, if a \Species definition sets
% \token{hasOnlySubstanceUnits} to \val{true}, then it must not have a value
% for \token{spatialSizeUnits}.  (\token{spatialSizeUnits} has
% been removed in SBML Level~2 Version~3.  References: L2V1 Section 4.6.4;
% L2V2 Section~\ref{sec:species-units}.)
