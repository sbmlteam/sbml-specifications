The top-level element within the MathML \token{math} element in a
\FunctionDefinition object definition must be either exactly one MathML
\token{lambda} element, or exactly one MathML \token{semantics} element
containing exactly one \token{lambda} element.  (References: SBML L3V1
Section~\ref{sec:function-definition-math}.)
