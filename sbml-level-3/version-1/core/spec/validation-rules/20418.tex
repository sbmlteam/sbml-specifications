A \UnitDefinition object must have the required attribute 
\token{id} and may have the following optional attributes:
\token{metaid}, \token{sboTerm} and \token{name}.  No other attributes are permitted.  
(References: SBML L3V1 Section~\ref{sec:unitdefinitions}.)
