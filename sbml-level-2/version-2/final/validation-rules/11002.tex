The order of subelements within \Constraint must be the following:
\token{math}, \token{message}.  The \token{message} element is optional,
but if present, must follow the \token{math} element.  (References: L2V2
Section~\ref{sec:constraints}.)
