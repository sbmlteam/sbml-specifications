Where the \token{units} attribute on a \Compartment having
\token{spatialDimensions} of \val{2} has not been set the units
are the \token{areaUnits} declared on the \Model.  If neither
value is set, unit consistency cannot be checked.  (References: 
L3V1 Section~\ref{sec:compartment-units}.)
