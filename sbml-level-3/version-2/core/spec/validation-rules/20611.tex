A \Species object having a value of \val{false} for its attribute
\token{boundaryCondition} cannot appear as a reactant or product in any
reaction if that \Species also has a value of \val{true} for its attribute
\token{constant}.  (References: SBML L3V1
Section~\ref{sec:species-constant}.)
