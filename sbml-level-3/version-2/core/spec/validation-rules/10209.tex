\removedRule
  {The arguments of the MathML logical operators \token{and}, \token{not}, \token{or}, and \token{xor} must evaluate to Boolean values.}
  {(References: SBML L3V1 Section~\ref{sec:mathmltype}.)}
