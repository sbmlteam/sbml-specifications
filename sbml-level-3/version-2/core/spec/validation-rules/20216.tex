The value of the attribute \token{conversionFactor} on a \Model object must
be the identifier of an existing \Parameter object defined in the \Model
object's \ListOfParameters.  (References: \tworefs{4.2}{\ref{sec:model}}.)
