Inside the \token{lambda} of a \FunctionDefinition, if a \token{ci} element
is not the first element within a MathML \token{apply}, then the
\token{ci}'s value can only be the value of a \token{bvar} element declared
in that \token{lambda}.  (References: L2V2
Section~\ref{sec:function-definition-math}.)
