Any \Compartment, \Species, \Parameter or \SpeciesReference object whose
identifier is the value of a \token{variable} attribute in an
\AssignmentRule object, must have a value of \val{false} for its
\token{constant} attribute.  (References: SBML L3V1
Section~\ref{sec:assignmentrule}.)
