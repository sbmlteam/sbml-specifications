An \EventAssignment object must have the required attribute
\token{variable} and may have the optional attributes \token{id}, \token{name}, \token{metaid} and
\token{sboTerm}.  No other attributes from the SBML Level~3 Core namespace
are permitted on an \EventAssignment object.  (References: \tworefs{4.12}{\ref{sec:events}}.)
